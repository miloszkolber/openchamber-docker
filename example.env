# `stat -c '%g' /var/run/docker.sock`
DOCKER_GID=109

# One persistent home for OpenChamber, OpenCode, and Docker client state.
DATA_PATH=./data

OPENCHAMBER_PORT=4098
TZ=Europe/Warsaw

# Empty means no UI authentication. Set a strong value before untrusted exposure.
OPENCHAMBER_UI_PASSWORD=
